# Clustering on Moon Dataset — K-Means vs Spectral Clustering

This project demonstrates how different clustering algorithms behave on non-linearly separable data using the classic "Two Moons" synthetic dataset.
We compare the performance of K-Means (a centroid-based algorithm) and Spectral Clustering (a graph-based algorithm) to visualize their strengths and weaknesses.
